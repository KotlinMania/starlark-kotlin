// port-lint: source src/values/layout/heap.rs
package io.github.kotlinmania.starlark_kotlin.values.layout

/**
 * Starlark heap implementation module.
 *
 * In the Rust source this file primarily declares submodules (e.g. `allocator`, `arena`, `repr`).
 * Kotlin does not have `mod` declarations; those submodules are represented by packages under
 * `io.github.kotlinmania.starlark_kotlin.values.layout.heap`.
 */

